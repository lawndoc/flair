program KindOfAdd(n : integer, m : integer);
    function add(n: integer, m: integer) : integer
    begin
        return if check() then
            n + m
        else
            2
    end;
    function check() : boolean
    begin
        return if works(true) then
            true
        else
            false
    end;
    function works(n : boolean) : boolean
    begin
        return n
    end;
begin
    return add(n, m)
end.
